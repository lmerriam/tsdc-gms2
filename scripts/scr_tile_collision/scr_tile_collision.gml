///scr_tile_collision(x,y)
//TODO: maybe need to delete this whole script if never used
var xx = argument0 div global.tilewidth;
var yy = argument1 div global.tileheight;

return (global.collision_tiles[# xx,yy]);
